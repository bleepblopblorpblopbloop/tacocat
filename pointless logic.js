 // this was suppposed to move the beer when the cat pushes it but it didn't work and the answert was much simpler
 // if (glasses.row === (player.row + (WIDTH * 0.1) && player.keyPressed() === 39)) {
 //     glasses.keyPressed();
 // } else if (glasses.row === (player.row - (WIDTH * 0.1) && player.keyPressed() === 37)) {
 //     glasses.keyPressed();
 // } else if (glasses.col === (player.col + (HEIGHT * 0.1) && player.keyPressed() === 38)) {
 //     glasses.keyPressed();
 // } else if (glasses.col === (player.col - (HEIGHT * 0.1) && player.keyPressed() === 40)) {
 //     glasses.keyPressed();
 // }


 // this adds a grid to the board
 // push()
 // strokeWeight(4)
 // for (let i = 0; i < 800; i += 80) {
 //     line(i + 80, 0, i + 80, 800);
 //     line(0, i + 80, 800, i + 80)
 // }
 // pop()




 let drinksArray = [];

 drink {
     x: ,
     y:
 }

 for (let i = 0; i <= drinksArray.length; i++) {

 }