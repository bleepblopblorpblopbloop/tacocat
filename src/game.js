class Game {
    drawGrid() {
        strokeWeight(2)
        rect(0, 0, WIDTH, HEIGHT)

        // push()
        // strokeWeight(4)
        // for (let i = 0; i < 800; i += 80) {
        //     line(i + 80, 0, i + 80, 800);
        //     line(0, i + 80, 800, i + 80)
        // }
        // pop()

    }

}